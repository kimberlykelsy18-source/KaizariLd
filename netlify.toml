[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Form notifications - all registration forms send to admin email
# Note: Form submissions are available in Netlify dashboard
# Email notifications should be configured in Netlify dashboard:
# Site settings > Forms > Form notifications
# Add notification to: admin@kaizarildinternational.com
